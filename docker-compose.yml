version: "3.9"

services:
  frontend:
    image: personal-injury-frontend
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
    volumes:
      - ./:/app:cached
      - /app/node_modules
    command: npm run dev
